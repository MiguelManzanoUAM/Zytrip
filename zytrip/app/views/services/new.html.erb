<%= render "layouts/header" %>
<section>
  <div class="create-edit-trip">
    <div class="create-edit-trip-box">
      <h2 class="create-edit-trip-title">Unas últimas preguntas antes de terminar</h2>
      <%= form_for(@service, url: services_path(@service)) do |f| %>
        <%= render partial: "service_form_fields", :locals => {:f => f} %>
        <div class="create-edit-trip-btns">
          <div class="row justify-content-center">
            <div class="col-7">
              <%= f.submit "Siguiente", class: "create-edit-trip-form-submit-btn" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</section>

<%= render "layouts/footer" %>
<% content_for :view_specific_css do %>
    <%= stylesheet_link_tag "main" %>
<% end %>