<%= render "layouts/sidebar" %>
<section>
  <div class="create-edit-styled-box">
    <h2 class="create-edit-header">Crear Información Adicional Usuarios</h2>
    <%= form_for(@additional_information, url: admin_additional_informations_path(@additional_information)) do |f| %>
      <%= render partial: "additional_information_form_fields", :locals => {:f => f} %>
      <div class="actions-btns">
        <div class="row">
          <div class="col-lg-6 col-md-12 col-sm-12 text-center create-edit-form-cancel-btn">
                <%= link_to "Volver", :back, class: "create-edit-form-cancel-link" %>
            </div>
            <div class="col-lg-6 col-md-12 col-sm-12 text-center ">
                <%= f.submit "Crear", class: "create-edit-form-submit-btn" %>
            </div>
          </div>
        </div>
        <% end %>
    </div>
</section>

<% content_for :view_specific_css do %>
    <%= stylesheet_link_tag "admin" %>
<% end %>